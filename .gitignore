# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# local env files
.env*.local
.env
.env.development
.env.production
.env.test

# supabase
supabase/.temp/
supabase/.branches/

# product image originals (regenerable via `npm run catalogue:images` —
# keeping the 82 MB of pre-normalization sources out of the repo)
public/images/_originals/

# backup des photos avant détourage (régénérable via `npm run images:transparent`)
public/images/_pre-transparent/

# vercel
.vercel

# guide de migration (contient une clé API)
MIGRATION-NOUVEAU-PC.md

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel

# Playwright MCP screenshots/logs (artefacts locaux)
.playwright-mcp/

# Claude Code (état local : worktrees, config agent)
.claude/

# fichiers de verrouillage Word/Office — ne JAMAIS versionner
# (leur présence signale surtout qu'un document est resté ouvert)
~$*

# documentation utilisateur : ~9 Mo de binaires non diffables, gardés hors dépôt
/Documentation FLUXITRON.docx
docs/*.docx
docs/captures/
docs/generate_manual.py
