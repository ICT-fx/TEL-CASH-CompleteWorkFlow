// Display helpers admin — réexportés depuis la source unique (lib/products)
// pour éviter toute divergence de la logique de grade / couleur.
export { normalizeGradeLetter, colorToCss } from '@/lib/products';
